#include <iostream>

using std::cout;

int main() {

    int slices; //declaration
    slices = 5; //initialization 
    cout << slices;
    slices = 5 + 1;
    cout << slices;
    int children = slices;
    cout << children;

    return 0;
}